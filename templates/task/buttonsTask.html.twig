{% if app.user %}
	<div class="container">
		<div class="row p-3 mb-2 bg-white text-dark rounded-pill">
			<div class="col-lg-3 col-12">
				<a href="{{ path('task_create') }}" class="btn btn-success col-lg-12 col-12">Créer une tâche</a>
			</div>
			<div class="col-lg-3 col-12">
				<a href="{{ path('task_list') }}" class="btn btn-info col-lg-12 col-12">Toutes les tâches</a>
			</div>
			<div class="col-lg-3 col-12">
				<a href="{{ path('task_list_done') }}" class="btn btn-secondary col-lg-12 col-12">Tâches terminées</a>
			</div>
			<div class="col-lg-3 col-12">
				<a href="{{ path('task_list_todo') }}" class="btn btn-secondary col-lg-12 col-12">Tâches à faire</a>
			</div>
		</div>
	</div>
{% endif %}
